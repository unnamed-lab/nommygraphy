import { LoadingScreen } from "@/components/loading-screen";

export default function DashboardLoading() {
  return (
    <LoadingScreen message="Loading your dashboard..." variant="default" />
  );
}
